<tr>
  <td><%= item.id %></td>
  <td><%= item.user_id %></td>
  <td><%= item.created_at.to_formatted_s(:long) %></td>
  <td><%= item.size %></td>
  <td><%= item.description %></td>
  <td><%= link_to 'Edit', edit_item_path(customer) %></td>
  <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>item