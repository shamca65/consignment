<script type="text/javascript">
	$(document).ready(function () {

    var url = "/items/pickups";
    var leftGrid = $('#jqxgrid');
    var rightGrid = $('#jqxgridRight');

    var moveItemsToStoreStock = function() {
      // get selected rows
		  var ROWINDEXES = leftGrid.jqxGrid('getselectedrowindexes');
		  var row = getRowData(ROWINDEXES);
		  //var value = rightGrid.jqxGrid('addrow', null, row);
    };

    var getRowData = function(rowIndexes) {
      var rows = new Array();

      for (var i = 0; i < rowIndexes.length; i++) {
        var datarow = generaterow(i);
        //rows.push(datarow);

      }
	  };

		var generaterow = function (id) {
			/*var row = {};
			row["ActivityID"] = id;
			row["Name"] = "Test record"
			row["Comments"] = "Generated by Javascript"
			row["Keywords"] = "JS TEST"
			return row;
			*/
			alert('row : ' + id);
		};

	  var removeSelectedRows = function () {
		  var ROWINDEXES = leftGrid.jqxGrid('getselectedrowindexes');
		  alert('rowindexes : ' + ROWINDEXES);
		  leftGrid.jqxGrid('deleterow', ROWINDEXES);
		  leftGrid.jqxGrid('clearselection');
	  };

    var JSONsource = {
        datatype: "json",
        datafields: [
          { name: 'id', type: 'integer' },
          { name: 'chk', type: 'string' },
          { name: 'description', type: 'string' },
          { name: 'size', type: 'string' },
          { name: 'gender', type: 'string' }
        ],
        id: 'id',
        url: url
      };

    var JSONdataAdapter = new $.jqx.dataAdapter(JSONsource);

	  leftGrid.jqxGrid({
      width: 500,
		  theme: 'energyblue',
      source: JSONdataAdapter,
      editable: true,
		  selectionmode: 'checkbox',
		  altrows: true,
        ready: function ()
        {
			    console.log('left grid is ready');
        },

      columns: [
        { text: 'Item ID', datafield: 'id', width: 75, editable: false },
        { text: 'Description', datafield: 'description', width: 250, editable: false },
        { text: 'Size', datafield: 'size', width: 50, editable: false },
        { text: 'Gender', datafield: 'gender', width: 75, editable: false }
      ]
    });

	  rightGrid.jqxGrid({
		  width: 500,
		  theme: 'energyblue',
		  source: JSONdataAdapter,
		  editable: true,
		  selectionmode: 'checkbox',
		  altrows: true,
		  ready: function ()
		  {
			  console.log('right grid is ready');
		  },

		  columns: [
			  { text: 'Item ID', datafield: 'id', width: 75, editable: false },
			  { text: 'Description', datafield: 'description', width: 250, editable: false },
			  { text: 'Size', datafield: 'size', width: 50, editable: false },
			  { text: 'Gender', datafield: 'gender', width: 75, editable: false }
		  ]
	  });

    var setBtnState = function () {
      var isValid = (selectedItems > 0);
      if (isValid) {
        $("#mybutton").jqxButton({ disabled: false});
      }
      else {
        $("#mybutton").jqxButton({ disabled: true });
      }
    };

    $("#mybutton").jqxButton({
      theme: 'energyblue',
      width: 200,
      height: 30
    });

    $('#mybutton').click(function () {
		  moveItemsToStoreStock();
      //removeSelectedRows();
    });

	});
</script>
<div class="col-md-12">
  <div class="col-md-6">
    <div id='jqxWidget' style="font-size: 14px; font-family: Verdana; float: left;">
      <div id="jqxgrid"></div>
    </div>
  </div>
  <div class="col-md-6">
    <div id='jqxWidgetRight' style="font-size: 14px; font-family: Verdana; float: left;">
      <div id="jqxgridRight"></div>
    </div>
  </div>

<FORM>
  <INPUT TYPE=BUTTON NAME="button" VALUE="Window" id="mybutton">
</FORM>

</div>