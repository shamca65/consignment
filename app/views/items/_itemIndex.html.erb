<tbody>
<% @items.each do |item| %>
  <tr >
    <td class="text-center h5 align-middle">
      <%= link_to edit_item_path(item.id) do %>
        <%= item_id_badge(item.id.to_s) %></td>
      <%end%>
    <td class="text-center h5 align-middle"><%= simple_date(item.takein_date) %></td>
    <td class="text-center align-middle h5"><%= gender_label(item.gender) %></td>
    <td class="text-center align-middle h5"><%= size_label(item.size) %></td>
    <td class="text-center align-middle h5"><%= type_label(item.item_type) %></td>
    <td class="text-left h5 align-middle"><%= item.description.truncate(75) %></td>
    <td class="text-left h5 align-middle">
      <%= link_to items_customers_path(item.customer.id) do %>
        <span><%= item.customer.full_name.truncate(50) %>  <i class="far fa-external-link fa-1x "></i></span>
      <% end %>
    </td>
    <td class="text-left h5 align-middle"><%= item.customer.phone %></td>
    <td class="text-center h5 align-middle"><%= number_to_currency(item.price, precision:2) %></td>
    <td class="text-center h5 align-middel">
        <%= link_to edit_item_path(item) do %>
          <i class="fal fa-edit fa-2x text-info"></i>
        <% end %>
      </div>
    </td>
    <td class="text-center h5 align-middle">
      <div class="itemDeleteIcon" >
      <%= link_to 'Delete', @item, method: :delete, data: { confirm: 'Are you sure?' } do %>
        <i class="fal fa-layer-minus fa-2x text-danger " ></i>
      <% end %>
      </div>
    </td>
  </tr>
<% end %>
</tbody>
</table>