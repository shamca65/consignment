<tbody>
<% @customers.each do |customer| %>
  <tr>
    <td><%= customer.id %></td>
    <td><%= customer.full_name %></td>
    <td><%= customer.phone %></td>
    <td><%= customer.email %></td>
    <td >
      <%= link_to ' ', items_customers_path(customer.id), class: 'fa fa-lg fa-list-ol icon' %>
    </td>
    <td>
      <%= link_to ' ', edit_customer_path(customer.id), class: 'fa fa-lg fa-id-card-o' %>
    </td>
  </tr>
<% end %>
</tbody>