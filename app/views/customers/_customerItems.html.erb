<% @customerItems.each do |item| %>
  <tr>
    <td><%= item.id %></td>
    <td><%= item.description %></td>
    <td><%= item.size %></td>
    <td><%= number_to_currency(item.price)%></td>
  </tr>
<% end %>
</tbody>