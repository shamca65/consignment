<style>
  .btn {
      padding: 0.7rem 1.6rem;
      font-size: 1.2rem !important;
  }
</style>
<nav class="navbar navbar-expand-sm fixed-top navbar-dark bg-dark ">
    <li class="navbar-brand text-lg-left">Acta Diurna</li>
    <% if controller_name != 'sessions' %>
    <div class="collapse navbar-collapse mx-auto " id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item btn btn-outline-primary btn-md <%= active?(root_path) %>">
          <a class="nav-link text-lg-center" href="<%= root_path %>">Home<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item btn btn-outline-secondary btn-md <%= active?(customers_path) %>">
          <a class="nav-link " href="<%= customers_path %>">Customers</a>
        </li>
        <li class="nav-item btn btn-outline-info btn-md <%= active?(items_path) %>">
          <a class="nav-link " href="<%= items_path %>">Items</a>
        </li>
      </ul>
      <div class="navbar-right">
        <% if user_signed_in? %>
          <div class="text-sm-center">
          <%= button_to('Sign out', destroy_user_session_path, method: :delete, class: "btn btn-md btn-outline-warning") %>
          </div>
        <% end %>
      </div>
    </div>
      <% end %>
  </nav>