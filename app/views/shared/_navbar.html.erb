<div class="table-responsive">
  <table class="table">
    <tr id="top-nav" class="center-cell" >
      <td><%= render 'shared/footerlinks' %></td>
      <td class="spacer"></td>
      <td class="spacer"></td>
      <td class="spacer"></td>
      <td class="mover-right" style="alignment: top">
      <div class="btn-group" role="group" aria-label="Navigation">
        <% if user_signed_in? %>
          <%= link_to 'Sign Out', destroy_user_session_path, type: "button", class: "btn btn-info" %>
        <%else %>
          <%= link_to 'Sign In', new_user_session_path, type: "button", class: "btn btn-warning" %>
        <%end %>
      </div>
    </td>
    </tr>
    </tbody>
  </table>
</div>