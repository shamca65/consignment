 <!-- Modal -->
 <%= form_with model: @pickupdates, url: '/pickupdates/new' do |d| %>
   <div class="md-form h3">
     <input placeholder="Selected date" type="text" id="datepicker"
            class="form-control datepicker h3"
            name="pickupdate[:current]">
   </div>
 <% end %>